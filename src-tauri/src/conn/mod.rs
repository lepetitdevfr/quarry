pub mod config;

// `ConnectionConfig` and `SslMode` don't exist yet — Task 3 defines them in
// `config.rs` and adds the re-export here. Until then this module only
// declares `config` so the crate keeps compiling.
